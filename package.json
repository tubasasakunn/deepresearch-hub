{
  "name": "deepresearch-hub",
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "reset": "wrangler d1 execute deepresearch_db --file=./drizzle/0000_initial_schema.sql --local"
  },
  "dependencies": {
    "hono": "^4.6.20",
    "drizzle-orm": "^0.39.1",
    "marked": "^4.0.10"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250129.0",
    "drizzle-kit": "^0.30.4",
    "typescript": "^5.7.3",
    "wrangler": "^3.107.2"
  }
}
